# Paperback - 版本 0.9.1

## 简介

Paperback 是一款轻量、快速且无障碍的电子书和文档阅读器，适合从普通读者到重度高级用户的各类人群。它专为屏幕阅读器无障碍访问、高速运行以及无臃肿体验而设计。

## 系统要求

Paperback 目前可在 Windows 10/11 以及所有现代版本的 ARM macOS 上运行。原生 iOS 和 Android 应用正在积极开发中，公共测试版本计划在 0.9.0 桌面版发布后不久推出，最终将实现覆盖四个平台的统一 1.0 版本。

## 功能

* 完全独立运行，无需在计算机上安装任何其他软件即可开始阅读。
* 速度极快，即使在老旧硬件上也能流畅运行。
* 简洁的选项卡式界面，允许您并排打开任意数量的文档。
* 为您打开的每个文档保存精确的阅读位置。
* 可选择记住程序关闭时打开的文档，并在下次启动时恢复。
* 包含与许多屏幕阅读器网页浏览模式类似的导航功能，可快速轻松地在文档中移动。
* 包含强大的查找对话框，支持历史记录和正则表达式等功能。
* 既可以完全便携运行，也可以安装并自动设置文件关联。
* 支持大量常见文件格式。

## 屏幕阅读器兼容性

Paperback 可与所有主流屏幕阅读器良好配合。不过，对于 JAWS 用户存在一个已知问题。

### JAWS 与盲文显示器

如果您将 JAWS 与盲文显示器配合使用，可能会发现使用显示器的导航键向前平移时长段落被截断。“朗读当前段落”命令也受到影响。这是 JAWS 处理 RICHEDIT50W 文本控件时的一个错误，并非 Paperback 本身的问题，而且鉴于 Vispero 对回应开源软件问题的积极性，找到修复方案花费了相当长的时间。

经过数月的等待，最终通过 JAWS 讨论组找到了解决办法：编辑 `paperback.jcf`，将“盲文显示与平移”设置为“可用时始终使用 DOM”。您还需要启用“按段落平移文本”，否则显示器会停留在当前活动段落而不会前进。完成这两项设置后，平移应该就能正常工作了。

## 当前支持的文件类型

Paperback 支持以下格式和扩展名：

* CHM 帮助文件（`.chm`）
* DAISY 图书（`.opf`、`.zip`）
* EPUB 图书（`.epub`）
* FB2 电子书（`.fb2`）
* HTML 文档（`.htm`、`.html`、`.xhtml`）
* Markdown 文档（`.md`、`.markdown`、`.mdx`、`.mdown`、`.mdwn`、`.mkd`、`.mkdn`、`.mkdown`、`.ronn`）
* Microsoft Word 文档（`.docx`、`.docm`、`.doc`）
* MOBI/Kindle 图书（`.mobi`、`.azw`、`.azw3`）
* OpenDocument 演示文稿（`.odp`、`.fodp`）
* OpenDocument 文本文件（`.odt`、`.fodt`）
* PDF 文档（`.pdf`）
* PowerPoint 演示文稿（`.pptx`、`.pptm`、`.ppt`）
* RTF 文档（`.rtf`）
* 纯文本和日志文件（`.txt`、`.log`）

## 键盘快捷键

Paperback 专为键盘优先使用而设计。以下是当前的快捷键。

以下快捷键适用于 Windows。若 macOS 有所不同，将在括号中注明等效按键——主要是因为在该平台上 Ctrl+G、Ctrl+W 以及 Alt+左/右箭头已被其他系统或应用约定占用。

### 文件菜单

* `Ctrl+O`：打开文档。
* `Ctrl+F4`（macOS：`Cmd+W`）：关闭当前文档。
* `Ctrl+Shift+F4`（macOS：`Cmd+Shift+W`）：关闭所有打开的文档。
* `Ctrl+Shift+T`：重新打开上次关闭的文档。
* `Ctrl+R`：显示“所有文档”对话框（来自“最近文档”）。
* `Ctrl+Q`：退出（仅限 Windows；在 macOS 上此功能位于应用菜单下）。

### 转到菜单

* `Ctrl+F`：显示查找对话框。
* `F3`（macOS：`Cmd+G`）：查找下一个。
* `Shift+F3`（macOS：`Cmd+Shift+G`）：查找上一个。
* `Ctrl+G`（macOS：`Cmd+L`）：转到行。
* `Ctrl+Shift+G`（macOS：`Cmd+Shift+L`）：转到百分比。
* `Ctrl+P`：转到页（当当前文档支持时）。
* `=`：朗读当前阅读百分比。
* `Alt+Left`（macOS：`Cmd+[`）：后退导航历史。
* `Alt+Right`（macOS：`Cmd+]`）：前进导航历史。
* `[`：上一章节。
* `]`：下一章节。
* `Shift+H`：上一个标题。
* `H`：下一个标题。
* `Shift+1` 至 `Shift+6`：上一个 1-6 级标题。
* `1` 至 `6`：下一个 1-6 级标题。
* `Shift+P`：上一页。
* `P`：下一页。
* `Shift+B`：上一个书签。
* `B`：下一个书签。
* `/`：设置临时书签。
* `\`：跳转到临时书签。
* `Shift+N`：上一个笔记。
* `N`：下一个笔记。
* `Ctrl+B`：跳转到所有书签和笔记。
* `Ctrl+Alt+B`：仅跳转到书签。
* `Ctrl+Alt+M`：仅跳转到笔记。
* `Ctrl+Shift+W`（macOS：`RawCtrl+Shift+W`，即物理 Control 键而非 Cmd）：查看当前位置的笔记文本。
* `Shift+K`：上一个链接。
* `K`：下一个链接。
* `Shift+G`：上一张图片。
* `G`：下一张图片。
* `Shift+F`：上一个图形。
* `F`：下一个图形。
* `Shift+T`：上一个表格。
* `T`：下一个表格。
* `Shift+S`：上一个分隔符。
* `S`：下一个分隔符。
* `Shift+L`：上一个列表。
* `L`：下一个列表。
* `Shift+I`：上一个列表项。
* `I`：下一个列表项。
* `Shift+,`：转到当前容器（列表或表格）的开头。
* `,`：越过当前容器（列表或表格）的末尾。

### 工具菜单

* `Ctrl+W`（macOS：`RawCtrl+W`，即物理 Control 键而非 Cmd）：显示当前文档的字数统计。
* `Ctrl+I`：显示文档信息。
* `Ctrl+T`：显示目录。
* `F7`：显示元素列表。
* `Ctrl+Shift+C`：打开所在文件夹。
* `Ctrl+Shift+V`：在 Web 视图中打开当前内容。
* `Ctrl+U`：在新标签页中查看文档源。
* `Ctrl+Shift+E`：导出文档数据（`.paperback`）。
* `Ctrl+Shift+I`：导入文档数据（`.paperback`）。
* `Ctrl+E`：将当前文档导出为纯文本。
* `Ctrl+Shift+B`：在当前选择/光标处切换书签。
* `Ctrl+Shift+N`：在当前选择/光标处添加或编辑书签笔记。
* `Ctrl+Alt+W`：切换自动换行。
* `Ctrl+Space`：播放/暂停音频朗读。
* `'`：音频朗读向前跳转。
* `;`：音频朗读向后跳转。
* `Ctrl+'`：增大音频跳转步长。
* `Ctrl+;`：减小音频跳转步长。
* `F11`（macOS：`RawCtrl+Ctrl+F`，即 Control+Command+F）：切换全屏。
* `Ctrl+,`：打开设置（macOS：偏好设置，位于应用菜单下）。
* `Ctrl+Shift+S`：切换睡眠定时器。

### 帮助菜单

* `Ctrl+F1`：显示关于对话框。
* `F1`：在默认浏览器中查看帮助。
* `Shift+F1`：在 Paperback 中查看帮助。
* `Ctrl+Shift+U`：检查更新。
* `Ctrl+D`：在默认浏览器中打开捐赠页面。

### 文档视图附加按键

* 选项卡控件上的 `Delete` / `数字键盘 Delete`：关闭选中的文档选项卡。
* 文档文本中的 `Enter` 或 `Space`：激活光标处的链接，或在表格标记处打开表格视图。
* 文档文本中的 `Shift+F10` 或菜单/应用程序键：打开上下文菜单。

## 支持的语言

Paperback 已被翻译成多种语言，并且数量在不断增加。完整列表如下。

要了解如何贡献翻译，请阅读我们的[翻译指南](translating.md)。

* 波斯尼亚语
* 捷克语
* 荷兰语
* 芬兰语
* 法语
* 德语
* 日语
* 波兰语
* 葡萄牙语（巴西）
* 俄语
* 简体中文
* 塞尔维亚语
* 西班牙语
* 越南语

## 鸣谢

### 开发

* Quin Gillespie：主要开发者及项目创始人。
* Aryan Choudhary：主要贡献者。

### 捐赠

以下人士曾为 Paperback 的开发进行过一定数额的捐赠。如果您进行捐赠，您的名字不会自动添加到此列表，我只会添加那些希望公开捐赠的人。

注意：我认为公开的 GitHub 赞助者自动有资格列入此列表。

* Alex Hall
* Brandon McGinty
* Brian Hartgen
* Debbie Yuille
* Devin Prater
* Felix Steindorff
* Hamish Mackenzie
* James Scholes
* Jayson Smith
* Jonathan Rodriguez
* Jonathan Schuster
* Keao Wright
* Michael Marshall
* Pratik Patel
* Roberto Perez
* Sean Randall
* Timothy Wynn
* Tyler Rodick

## 更新日志

### 版本 0.9.1

* 书签和笔记声音现在可在 macOS 上播放。
* DAISY 图书现在可在 macOS 上播放音频，而不是在静音状态下打开并跟踪其时间轴。
* 修复了 RTF 文档中弯引号、长破折号及类似字符消失，导致周围单词连在一起的问题。
* 修复了 RTF 图片将其原始数据作为乱码文本泄漏到文档中的问题。
* 修复了“最近文档”子菜单保留过期条目直到其他操作触发重建的问题。
* 键盘加速键重新出现在所有翻译中，因此俄语菜单再次拥有键盘访问功能。
* 大型 CHM 文档的打开速度现在最多可提升七倍。
* 打开的文档现在会注册到 Windows 中，因此它们会显示在任务栏跳转列表和开始菜单的最近列表中。
* “选项”已更名为“设置”，与移动应用以及 macOS 上的平台惯例保持一致。
* Paperback 现在会记住运行之间的窗口位置、大小和最大化状态。
* 复数形式现已翻译，因此计数类消息在需要多种复数形式的语言中能正确显示。
* 选择 DAISY 图书的 ncc.html 现在会打开完整的音频书，而不仅仅是其文本。
* “自定义键盘快捷键”对话框中的操作名称现在可以翻译。
* 文档标题现在位于标题栏的最前面，以便在任务栏和 Alt+Tab 中区分打开的书籍。
* 更新对话框现已翻译。

### 版本 0.9.0

#### 新增

##### 常规

* 一个名为 pb 的命令行工具，可快速将 Paperback 支持的任何格式转换为 HTML、Markdown 或纯文本。
* 一个选项，用于重新加载已被其他程序在磁盘上修改的文档。
* 查看源代码选项，可在新标签页中打开文档源，例如对编辑 Markdown 很有用。
* 文档文本现在进行了分页，这意味着您现在可以在几秒钟内加载数千万字的书籍。如果发现任何异常，请报告。

##### 平台支持

* ARM64 Windows 支持！
* 原生 macOS 支持！
* 全屏切换。

##### 所有文档对话框

* 定位按钮，用于定位仅路径发生更改的缺失书籍。
* 状态筛选器和状态栏，您可以按文档状态筛选，并查看显示和选中的文档数量。
* `Ctrl+Shift+A` 快捷键，用于取消选择所有文档。

##### 选项与可读性

* 一个可读性选项卡，包含以下选项：
    * 自动换行（从常规选项卡移入）；
    * 内联渲染表格（此版本新增，见下文）；
    * 字体；
    * 背景颜色；
    * 行距；
    * 段落间距；
    * 字距；
    * 文本对齐。
* 自动换行菜单项及其热键。
* 一个用于决定表格显示方式的开关，并统一了跨文档的表格显示方式。

##### 导航

* 支持按容器导航。
* 一个选项，可在行间导航时自动将光标移至行首，类似于屏幕阅读器的浏览模式。
* 等号快捷键，用于朗读您在文档中的当前百分比。

##### 书签

* 临时书签：每个文档可以有一个，并且会持久保存。使用斜杠设置，使用反斜杠跳转。

##### 字数统计

* 字数统计对话框中新增预计阅读时间，并允许设置阅读速度以使该指标真正有用。
* 打开字数统计对话框时，如果有选区处于活动状态，现在会显示所选内容的字数。

##### 键盘快捷键

* 能够通过一个简单的对话框自定义应用中的每个键盘快捷键。
* 一个可配置的键盘快捷键，用于从系统托盘恢复 Paperback。

##### 语言

* 荷兰语、芬兰语和波兰语。

##### 导出

* 扩展了导出菜单项，除了纯文本外，还允许导出为 HTML 和 Markdown。

##### 更新器

* 为更新进行中对话框添加了取消按钮。
* 更新器现在会验证下载的文件未被篡改。

##### Web 视图

* Web 视图现在会在您当前的阅读位置打开。

##### DAISY 图书

* 支持 DAISY 2.0 图书。
* 支持 DAISY 2.02 音频播放。

##### 有声书

* 能够播放有声书，目前支持 DAISY 音频（包括 DAISY 音频+文本）和音频文件压缩包。
* 用于播放/暂停朗读、前后跳转以及调整跳转步长的键盘快捷键和菜单项。
* 选项：将阅读光标同步到音频播放、设置音频跳转步长，以及选择在章节末尾继续跳转是否进入下一章。

##### CHM 文档

* 支持列表、列表项、图形和图片。

##### PowerPoint

* PowerPoint 文档现在支持表格。

#### 修复

##### 常规

* 使用旧版 CJK 编码（如 GBK、Big5 和 Shift_JIS）的文档现在可以正确渲染，而不会显示为一堆乱码。
* “重新打开上次关闭的文档”试图重新打开捆绑的 readme。
* 重新启动 Paperback 后，所选选项卡未正确获得焦点。
* Paperback 对 Windows 网络驱动器上文件的处理：按下“在文件夹中显示文件”现在可以在网络存储上正确定位文件，路径中不再包含奇怪字符。
* `.paperback` 文件在文档恢复时将不再被强制加载；相反，当发现此类文件时会询问确认。
* “打开所在文件夹”现在会在资源管理器中定位到给定文件。
* 打开 readme 现在会遵循您选择的语言。
* Paperback 的用户界面现在在高 DPI 显示器上正确缩放。
* 在 Paperback 中打开帮助时，菜单现在会正确更新，焦点会移至文本控件。
* 在 Windows 上切换到了一种更安全的 IPC 方法。
* 切换选项卡时现在会朗读活动文档的标题。
* 通过将内部每字符索引表的大小减半，减少了大型文档的内存使用量。

##### 所有文档对话框

* Esc 键无法关闭“文档信息”和“所有文档”对话框。
* 从“所有文档”对话框关闭文档后，标题栏未更新。
* 通过 Shift+F1 打开时，Readme.html 将不再被添加到所有文档列表中。
* 从最近文档对话框中移除文档现在也会关闭其活动选项卡。
* 移除文档后，搜索筛选器现在会被保留。

##### 导航

* 页面导航在某些情况下朗读错误的行文本。
* 转到行、转到页和转到百分比在大型文档中将光标置于错误位置。
* 在大型文档中，查找和查找下一个未遵守已加载的文档窗口。

##### 书签

* 书签/笔记声音现在应当仅在您导航到包含它们的单词时才正确播放。

##### 可读性

* 应用自动换行会将您弹到文档开头。

##### Web 视图

* Web 视图对话框不可调整大小，并且初始弹出尺寸非常小。
* 图片现在应能在嵌入式 Web 视图中正确显示。

##### 更新器

* 更新器现在能正确显示发行说明中 Markdown 代码标签的内容。

##### DAISY 图书

* DAISY 图书在状态栏中显示不正确的信息。
* 加载带有虚假编码声明的 DAISY 图书。

##### RTF 文档

* 解析包含非拉丁字符的 RTF 文档。
* RTF `\pict` 组，嵌入式图片数据不再泄漏到文档文本中。

##### Mobi/AZW3 图书

* Mobi 图书中的 Filepos 锚点会拆分 HTML 标签并在图书文本中产生垃圾内容。
* 旧版 Mobi 图书中的链接。
* 大幅改进了 AZW3 解析。

##### Word 文档

* 具有区域设置特定样式名称的 Word 文档无法正确渲染其标题。

##### HTML/XHTML 文档

* dl、dt 和 dd 元素在 XHTML 文档中不产生换行符。

##### PDF 文档

* 对于标记错误的 PDF，Paperback 现在会回退到纯文本提取。
* 标题和/或书签中包含控制字符的 PDF 文档在打开时不再导致 Paperback 崩溃。

### 版本 0.8.5

* 为 epub 图书添加了页面支持。
* 添加了对加密 Microsoft Office 文档的支持。目前支持旧版 Word、新版 Word 和新版 PowerPoint，旧版 PowerPoint 计划在未来支持。
* 添加了对旧版 Microsoft Word 文档（*.doc）的支持！
* 添加了对旧版 PowerPoint 演示文稿（*.ppt）的支持！
* 添加了对 mobi 和 AZW3 图书的支持！
* 添加了对带标签的 PDF 文件的支持！
* 添加了 Ctrl+Q 快捷键退出应用。
* 添加了对 Bookshare 压缩图书（DAISY 和 Word）的支持！
* 嵌入式图片的替代文本现在应能正确显示。
* CHM 文档现在正确支持内部链接导航。
* 修复了书签声音在段落开头而非书签位置触发的问题。
* 修复了转到页相差 1 的问题。
* 修复了 Esc 键无法关闭“打开方式”对话框的问题。
* 修复了右键或应用程序键无法弹出阅读器上下文菜单的问题。
* 修复了从命令行打开文档时有时聚焦到错误文档的问题。
* 仅包含图片的 PDF 再次被检测到并提醒您其存在。
* 现在可以分别使用 G/Shift+G 和 F/Shift+F 在图片和图形之间导航。
* Paperback 现在会遵循您的应用程序深色模式设置。
* 移除了 DAISY XML 支持，因为不再需要。
* 在目录树中切换回原生 Win32 首字母导航。
* 错误加载对话框现在显示更详细的错误消息。
* Web 视图现在打开得更快、更流畅。

### 版本 0.8.2

* 为 RTF 文档添加了页面支持！
* 修复了在包含外部链接的 epub 中打开 Web 视图会自动激活这些链接的错误。
* 修复了 RTF 解析器在极少数情况下不会在单词之间添加空格的错误。
* 修复了某些 PDF 文档中段落被拆分为多个短行的问题。
* PDF 文档现在具有基本的链接和标题导航支持！
* RTF 制表符和换行符现在完全按照文档中的样式渲染。
* 切换回经过验证的 pdfium 库来解析 PDF，使 PDF 渲染再次变得更加可靠。

### 版本 0.8.1

* 添加了 Ctrl+Shift+T 以重新打开上次关闭的文档。
* “所有文档”对话框现在支持同时选择多个文档打开。
* 修复了 RTF 解析器的几个错误。
* 修复了通过第二个 Paperback 实例打开文件时，包含非 ASCII 字符（如波斯尼亚语 š、č、ć、ž）的文件路径损坏的问题。
* 修复了 PDF 文本读取顺序错误以及大写单词周围间距不正确的问题。
* 修复了打开大文件时文档加载缓慢的问题。
* 修复了确认对话框中“是/否”按钮的本地化问题。

### 版本 0.8.0

* 添加了日语、简体中文和越南语翻译！
* 添加了自动更新程序，现在会替换您当前安装的 Paperback 版本，而不仅仅是下载新版本！
* 添加了到达书签或笔记时的可选声音反馈，感谢 Andre Louis 提供的声音！
* 添加了 RTF 文档支持！
* 添加了对 DAISY XML 文档的支持。
* 添加了对 Flat Open Document Text 文件的支持！
* 添加了对 Flat Open Document 演示文稿的支持！
* 添加了对分隔符的支持，使用 S 和 Shift+S。
* 任何超过 300 个字符的移动现在都会自动添加到导航历史记录中。
* 修复了从系统托盘恢复 Paperback 窗口的问题。
* 修复了 Markdown 文档在 Web 视图中显示原始文本而非渲染后的 HTML 的问题。
* 修复了 Markdown 文件中表格渲染不正确的问题。
* 仅包含图片的 PDF 现在会在您尝试加载时警告其存在。
* 现在可以在检查更新时检查新的开发版而非稳定版。
* 在 Paperback 可执行文件中正确嵌入版本信息。
* 将选项对话框拆分为选项卡，以便使用和导航。
* 切换到 Hayro 解析 PDF，带来更高的可靠性、速度，并减少 DLL 数量。
* 用 Rust 重写了整个应用。新代码库更安全，加载文档更快，更易于维护和扩展。
* 文本控件的上下文菜单现在会包含阅读器特定的操作，而不是剪切和粘贴等通用项目。

### 版本 0.7.0

* 为 HTML 和 XHTML 文档添加了表格支持！使用 T 和 Shift+T 在表格之间导航，按 Enter 可在 Web 视图中查看一个表格。
* 添加了基本的 Web 渲染功能！按 Ctrl+Shift+V 可在基于 Web 的渲染器中打开文档的当前部分，适用于复杂格式或代码示例等内容。
* 添加了俄语翻译，感谢 Ruslan Gulmagomedov！
* 为“所有文档”对话框添加了“全部清除”按钮。
* 更新检查器现在会在新版本可用时显示发行说明。
* 修复了从系统托盘恢复窗口的问题。
* 修复了确认对话框中“是/否”按钮的翻译问题。
* 修复了以管理员身份运行时加载配置的问题。
* 修复了 XML 和 HTML 文档中的注释处理。
* 修复了 Epub 2 图书中的目录解析。
* 修复了在目录中导航到具有相同字母的下一个项目的问题。
* 修复了使用下一个/上一个按钮时查找对话框未正确隐藏的问题。
* 修复了 epub 目录偶尔会将您带到错误项目的问题。
* 修复了 XML、HTML 和 pre 标签中的各种空白处理问题。
* 修复了链接导航中的差一错误。
* 修复了某些书籍行尾有多余空白的问题。
* 修复了各种解析器问题。
* 当没有文档打开时，与书签相关的菜单项以及元素列表现在会被正确禁用。
* 改进了各种文档格式中的列表处理。
* 改进了贡献者的翻译工作流程。
* 进行了许多内部重构，将应用程序的大部分业务逻辑从 C++ 迁移到 Rust，以提高性能和可维护性。

### 版本 0.6.1

* 添加了受密码保护的 PDF 支持！
* 添加了一个非常基本的转到上一个/下一个位置功能。如果您在内部链接上按 Enter 并且它移动了光标，该位置现在会被记住，并可通过 Alt+左/右箭头导航。
* 添加了元素列表！目前它仅显示文档中所有标题的树形结构或链接列表，但未来有计划扩展。
* 添加了一个选项，使 Paperback 默认以最大化模式启动。
* 修复了某些 Epub 文档中链接无法正常工作的问题。
* 修复了解析包含相对路径的 Epub 目录的问题。
* 修复了某些 epub 文档不显示标题或作者的问题。
* 修复了某些 epub 章节标题在目录对话框中未正确显示的问题。
* 修复了无法使用空格键激活目录对话框中的确定/取消按钮的问题。
* 改进了 Word 文档中标题的处理。
* 当您尝试打开最近文档对话框但列表为空时，现在会得到语音反馈。

### 版本 0.6.0

* 在选项对话框中添加了一个新选项，以更紧凑的形式显示“转到”菜单，默认选中。
* 添加了一个使按结构元素导航循环的选项。
* 在工具菜单中添加了一个选项，用于打开当前聚焦文档所在的文件夹。
* 添加了一个简单但非常有效的更新系统。
* 添加了基本的睡眠定时器功能，可通过 Ctrl+Shift+S 访问。
* 添加了对解析 FB2 电子书的支持！
* 添加了对解析 OpenDocument 演示文稿的支持！
* 添加了对解析 OpenDocument 文本文件的支持！
* 书签现在可以标记整行，或仅标记某些指定文本。如果您放置书签时没有活动选区，行为与 0.6 之前相同，将标记整行。但是，如果您选择了一些文本，则只有该文本会被包含在书签中。
* 书签现在可以附带可选的文本笔记！使用 N 和 Shift+N 在包含笔记的书签之间导航，或使用特定热键弹出包含所有书签、仅笔记或仅非笔记的书签对话框。
* 书签对话框中的书签将不再带有烦人的“书签 x”前缀。
* 包含伪装成 XML 的 HTML 内容的 Epub 图书现在将被正确处理。
* 修复了加载大型 Markdown 文档的问题。
* 修复了在目录树视图中按空格键激活确定按钮的问题。
* 修复了 HTML 和 XHTML 文档中 pre 标签开头空白处理的问题。
* 修复了返回 Paperback 窗口时文本控件有时无法重新获得焦点的问题。
* 修复了转到百分比对话框中文本字段未更新滑块值的问题。
* 修复了 Markdown 文档中自定义 HTML ID 的渲染。
* Markdown 代码块内的 HTML 现在会正确渲染。
* 如果使用命令行参数加载书籍时已有 Paperback 实例在运行，如果文档加载超过 5 秒，您将不再收到错误。
* 如果以管理员身份运行 Paperback，配置现在会被正确加载和保存。
* 现在可以直接从书签对话框中删除书签。
* 现在可以导入和导出特定文档的书签和阅读位置。生成的文件以原文件命名，扩展名为 .paperback。如果在加载文件时在同一目录中发现此类文件，它将自动加载。否则，您可以使用工具菜单中的项目手动导入。
* 文档内的链接现已完全支持！使用 K 和 Shift+K 在它们之间前后移动，按 Enter 打开/激活。
* 进行了许多内部重构，使应用更快，二进制文件更小。
* Markdown 内容现在经过预处理，以符合 CommonMark 规范后再渲染。
* 按列表及其项目导航现已完全支持！使用 L 和 Shift+L 按列表本身导航，使用 I 和 Shift+I 遍历列表项。
* 除了普通 Delete 键外，数字键盘 Delete 现在也可用于从选项卡栏移除文档。
* Paperback 现在可以选择最小化到系统托盘！此选项默认关闭，但开启后，系统菜单中的最小化选项会将 Paperback 放入托盘，可通过点击生成的图标恢复。
* Paperback 现已完全可翻译！它支持的语言列表目前还比较小，但正在不断增长！
* Paperback 现在有了官方网站，地址为 [paperback.dev](https://paperback.dev)！
* PPTX 文档现在将显示一个包含所有幻灯片的基本目录。
* 打开文档的完整路径现在将显示在文档信息对话框中。
* 安装程序现在包含一个选项，可在安装后在浏览器中查看 readme。
* 最近文档列表已大幅扩展！它不再只是显示您最近打开的 10 个文档，而是显示可自定义数量的文档，其余曾经打开过的文档可通过一个小对话框访问。
* 对解析器进行了各种小改进，包括在 PPTX 演示文稿的幻灯片之间放置空行，修复 Word 文档中段落内换行符的处理，以及为列表项添加项目符号。

### 版本 0.5.0

* 添加了 Microsoft Word 文档支持！
* 添加了对 PowerPoint 演示文稿的支持！
* 修复了没有文档打开时某些菜单项未禁用的问题。
* 修复了转到百分比滑块的方向。
* 修复了 Epub 图书中具有 URL 编码文件路径和/或片段 ID 的目录。
* 修复了 XHTML 标题以奇怪方式剥离空白的问题。
* 修复了 HTML 文档中嵌套 pre 标签内的空白处理。
* HTML 和 Markdown 文档现在支持目录功能！当您加载 HTML/Markdown 文档时，Paperback 将根据文档中标题的结构构建自己的目录，并在 Ctrl+T 对话框中显示给您。
* HTML 文档现在将使用 title 标签中设置的标题（如果存在）。否则，将继续使用不带扩展名的文件名。
* 从 UniversalSpeech 切换到使用活动区域来报告语音。这意味着不再随程序附带屏幕阅读器 DLL，并且将支持更多屏幕阅读器，例如 Microsoft 讲述人。
* 切换了 zip 库，以允许打开更广泛的 epub 图书。
* 询问您是否要将文档作为纯文本打开的对话框已完全重做，现在允许您以纯文本、HTML 或 Markdown 格式打开文档。
* 转到百分比对话框现在包含一个文本字段，允许您手动输入要跳转到的百分比。
* HTML 解析器现在将 dd、dt 和 dl 识别为列表元素。
* Epub 图书中的目录将再次被精确保留。
* 在剥离空行时现在会考虑 Unicode 不间断空格。
* 您将不再每次加载无法识别的文件时都被询问如何打开，只在第一次询问。

### 版本 0.4.1

* 向安装程序添加了可选的开始菜单图标。
* 目录在几种情况下现在应该更简洁，例如，如果子项和父项在相同位置具有相同文本，您现在只会看到父项。
* 修复了某些 CHM 文档中的目录。
* 修复了 Epub 3 图书中包含绝对路径的目录。
* CHM 文档现在应显示元数据文件中设置的标题。

### 版本 0.4.0

* 添加了 CHM 文件支持！
* 添加了书签支持！您可以在任意数量的文档中拥有任意数量的书签。您可以使用 B 和 Shift+B 在它们之间前后跳转，使用 Ctrl+Shift+B 设置一个，使用 Ctrl+B 弹出对话框跳转到特定书签。
* 在便携式 zip 文件旁边添加了安装程序！安装程序会将 Paperback 安装到您的 Program Files 目录，并自动为您设置文件关联。
* 带有 BOM 的文本文件现在应能正确解码，BOM 也将不再显示在文本开头。
* 向状态栏添加了更多信息。它现在会显示您的当前行、字符和阅读百分比。
* HTML 注释以及 script 和 style 标签的内容将不再显示在文本输出中。
* 如果在命令行中向 Paperback 传递相对路径，现在会正确解析。
* 百分比移动现在由自己的基于滑块的对话框处理，可通过 Ctrl+Shift+G 访问。
* 没有已知标题或作者的文档现在将始终有一个默认值。
* 位置保存逻辑现在更加智能，只应在绝对必要时才写入磁盘。
* 关闭 Paperback 时聚焦的文档现在会在应用程序重启后记住。
* 转到行和转到页对话框中的输入现在应进行更严格的清理。
* 修复了 epub 3 图书中清单包含相对路径的目录导航。

### 版本 0.3.0

* 修复了 epub 图书中具有 URL 编码清单的目录。
* 修复了包含多字节 Unicode 字符的 HTML 文档中的标题导航。
* 修复了由于 wxWidgets 回归导致的标题较长的文档中 CPU 使用率过高的问题。
* 修复了加载 UTF-8 文本文件的问题。
* 修复了 Epub 图书中嵌套目录项将光标置于错误位置的问题。
* 修复了某些情况下应用程序退出时崩溃的问题。
* 在选项对话框中添加了一个复选框以启用或禁用自动换行！
* 现在可以向 Paperback 的开发捐赠，既可以通过帮助菜单中的新捐赠项，也可以通过 GitHub 仓库主页底部的赞助此项目链接。
* Markdown 文档现在将始终具有标题，并且 Paperback 现在应该能够加载几乎任何 Markdown 文件。
* PDF 文档现在将始终具有标题，即使元数据缺失。
* 将 PDF 库切换到 Chromium 使用的库，全面提高了 PDF 解析的可靠性。
* 您现在一次只能运行一个 Paperback 实例。在已运行时使用文件名运行 paperback.exe 将在已运行的实例中打开该文档。
* 您现在可以在选项卡控件中按 Delete 键关闭文档。

### 版本 0.2.1

* 在转到页对话框中为页面标签添加了总页数。
* 允许从文档内容切换到已打开文档列表。
* 修复了如果您有足够多的最近文档，标题快捷键有时会打开最近文档的问题。
* Paperback 现在会从文本输出中移除不必要的软连字符。
* 修复了标题导航有时将您置于错误字符的问题。

### 版本 0.2.0

* 添加了 Markdown 文档支持！
* 添加了 PDF 文档支持，包括页面间导航的能力！
* 添加了用于在 HTML 内容（包括 epub 图书和 Markdown 文档）中按标题导航的按键。这些按键设计为与屏幕阅读器类似。
* 修复了加载清单中具有 URL 编码文件名的 epub 的问题。
* 修复了加载内部嵌有 XHTML 的 epub 3 图书的问题。
* 如果文档不支持目录或章节，现在会朗读一条消息，而不是禁用菜单项。
* 添加了最近文档菜单！它目前存储您最近打开的 10 个文档，在其中一个上按 Enter 将打开以供阅读。
* 完全重写了查找对话框，使其更易于使用，同时添加了最近 25 次搜索的历史记录和正则表达式支持！
* 以前打开的文档现在会在应用程序重启后记住。这可以通过工具菜单中的新选项项进行配置。
* 添加了 Shift+F1 直接在 Paperback 中打开 readme。

### 版本 0.1.0

* 初始版本。
